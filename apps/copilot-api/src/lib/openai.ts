/**
 * LLM client singletons for AI Copilot (Plan 07).
 * Primary: OpenAI (gpt-4.1-mini)
 * Fallback: Google Gemini (gemini-3.5-flash)
 *
 * Initialized lazily — only imported when Copilot module is loaded.
 */

// TODO (Plan 07): Initialize OpenAI and Google Generative AI clients
// import OpenAI from 'openai';
// import { GoogleGenerativeAI } from '@google/generative-ai';
// import { config } from '../config/env.js';
//
// export const openai = new OpenAI({ apiKey: config.OPENAI_API_KEY });
// export const gemini = new GoogleGenerativeAI(config.GOOGLE_AI_API_KEY);

export {};
