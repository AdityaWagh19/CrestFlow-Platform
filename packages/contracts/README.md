# CrestFlow Smart Contracts

Algorand smart contracts for CrestFlow, written in Puya Python.

## Contracts

- `escrow/` — x402 micropayment escrow (P1)
- `crest_token/` — ARC-20 CREST utility token (Phase 3)

## Setup

```bash
pip install -e ".[dev]"
```

## Testing

```bash
pytest tests/
```
